// const initialState = {
//     currentCategory: '',
// };

// export default (state = initialState, { type, payload }) => {
//     switch (type) {
//         case 'SET_CATEGORY':
//             return { ...state, currentCategory: payload };

//         default:
//             return state;
//     }
// };
